import { createTheme } from '@mui/material/styles'

const appTheme = createTheme({
  palette: {
    primary: {
      main: '#EE0000',
      dark: '#860000'
    },
    secondary: {
      main: '#EAD90F'
    },
    headers: {}
  }
})

export default appTheme
